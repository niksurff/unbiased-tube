{
  "private": true,
  "name": "@niksurff/unbiased-tube",
  "version": "0.1.0",
  "description": "A browser extension that removes popularity counts (views, likes, subscribers etc.) from popular video sites (i.e. YouTube).",
  "main": "",
  "scripts": {
    "test": "web-ext lint --source-dir extension",
    "dev": "web-ext run --source-dir extension",
    "build": "web-ext build --source-dir extension --artifacts-dir dist"
  },
  "keywords": [],
  "author": "Nik Surff (https://www.lazylife.ninja)",
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^2.8.0"
  }
}
